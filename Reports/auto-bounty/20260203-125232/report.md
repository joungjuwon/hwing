# AutoBounty Report (report-only)

- Timestamp: 2026-02-03 12:52 KST
- Project: `C:\Users\User\Documents\GitHub\hwing`
- Unity: 6000.2.15f1 (URP)

## 1) Snapshot
- Git branch: see `git-branch.txt`
- Git status: `git-status.txt`
- Git diff stat: `git-diff-stat.txt`

## 2) Unity batchmode run
Command (approx):
- `Unity.exe -batchmode -nographics -projectPath <proj> -quit -logFile <report>\unity-batch.log`

Artifacts:
- `unity-batch.log`
- `Editor.log` (copied from `%LOCALAPPDATA%\Unity\Editor\Editor.log`)
- `Editor.tail.txt`
- `Editor.errors.txt`

### Observed
- Batch log ends with: `Exiting ... return code 1` (see `unity-batch.log`).
- Batch log itself contains little diagnostic detail; the actionable signals are in `Editor.errors.txt`.

## 3) Findings (candidate issues)
### F1) Unity Connect / Cloud Project ID request failing (HTTP 403)
Evidence (from `Editor.tail.txt` / `Editor.errors.txt`):
- `Project ID request failed ... HTTP error code 403`
- `Unknown Unity Connect error (400) ... legacy/v1/projects/<guid>`

Impact:
- Can make headless/batch runs unstable or exit non-zero depending on Services state.

Suggested next actions:
- In Unity Editor: **Project Settings → Services**
  - confirm the account has access to the linked Cloud Project, or
  - disconnect/disable Services for this project (for local-only workflows), or
  - regenerate/clear Cloud Project ID if it’s pointing to a project you can’t access.

### F2) Shader fallback not found warnings
Evidence:
- `Shader 'Standard': fallback shader 'VertexLit' not found`
- `Shader 'Hidden/Internal-GUIRoundedRect' ... fallback ... not found`

Impact:
- Usually warning-level, but can indicate pipeline/package mismatch or missing built-in shaders.

Suggested next actions:
- Verify URP + package versions are consistent.
- Confirm `ProjectSettings/GraphicsSettings.asset` and URP asset assignments.

### F3) D3D12 info queue interface not available (0x80004002)
Evidence:
- `d3d12: failed to query info queue interface (0x80004002).`

Impact:
- Typically harmless; diagnostic interface missing. Worth noting only if rendering issues occur.

### F4) Bee caching client failure line
Evidence:
- `Failure while invoking caching client ...`
- `move_path failed: No error`

Impact:
- Can slow builds or cause intermittent compile pipeline hiccups.

Suggested next actions:
- If persistent: clear `Library/Bee` (manual, after confirming you’re okay with reimport time).

## 4) Next run improvements
To make the run more deterministic and useful (still report-only):
- Add a custom `-executeMethod` that waits for script compilation/import completion, then prints a clear PASS/FAIL summary.
- Optionally disable Services checks during batch runs (if possible for your workflow).

---
Generated by AutoBounty (local runner)
